Select a folder
	-checks for the number of files in the Folder
		if the folder is empty > print folder is empty
		else
		- Open the Folder
		- Select a file (Type doest matter)
			-Check if it has the tag 
			if it has the tag rename it to the current name minus the TAG
		- increments the counter by 1
		- Select next file
	-If no more files remain END